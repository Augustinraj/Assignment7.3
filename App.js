//declaring a variable named count//
var count = 0;
//function add one//
function addOne(){
    //this is used to increment the count each time we click on button//
  count++;
    //Displays the incremented values in the console//
  console.log(count);
}